# 后端核心实现记录

本仓库汇集此前分别开发的 Codex 源码运行时与 后端 业务核心。

## 来源与布局

Codex 来自 openai/codex 的 9e22e74e8dcab53f8bf1799c0eed1f9834c32f1a，
原始树为 9050fe9e982cdb042f707ee3169046b8e0cf9382。完整源文件和许可说明
保留在 codex-runtime/；导入时未修改其内核代码。

后端 保留了原有 v1 候选契约和治理工具，同时包含新增的 v2 核心业务实现。
原本独立的本地仓库均保留，本仓库是之后统一开发、提交与构建的入口。

## 已实现

- v2 JSON Schema 运行时校验，项目、任务、运行、尝试、审批、验收与事件。
- SQLite 原子保存状态、事件和幂等回执；版本冲突检查与历史尝试保留。
- Codex App Server stdio 协议、执行请求、单次命令/文件审批与取消。
- 断连和不确定结果保留为 recovery_required，不自动重发任务。
- 正常执行完成进入 awaiting_review，人工接受后才进入 completed。
- 带认证的本机 HTTP API、数据目录锁和退出清理。

## 验证范围

2026-09-17，在迁入前的原工作区通过 65 项后端测试和全部治理检查，
源码运行时也通过本地模拟模型的执行、验收、历史重读与取消联调。
归并后已重新通过后端 65 项、前端基座 12 项测试以及两个模块的治理检查。
新后端也通过了原源码树对应的既有构建产物联调，记录见 validation.json。

重跑命令在 README.md 中。源码构建不包含模型权重；真正使用需要配置模型认证。
业务界面、任务 DAG、科学结果自动验收、实验与产物版本关系和多 Agent 调度尚未实现。
