# Mathmodel Agent 前端

这是统一仓库的前端目录，现有内容为工程规范、校验工具及其测试。
当前尚无业务页面，不表示完整前端已经实现。

从本目录执行 npm test 与 npm run verify 可检查当前基座。
自动化入口位于项目根的 .github 目录；本模块内保留的工作流文件仅作为原基座记录。

本模块和后端在同一仓库维护，业务交互通过版本化契约进行。
后续在这里实现工作台，接入项目、任务、执行进展、产物和人工验收。
